apiVersion: apps/v1
kind: Deployment
metadata:
  name: kaimono-postgres
  labels:
    db: kaimono-postgres
spec:
  selector:
    matchLabels:
      db: kaimono-postgres
  template:
    metadata:
      labels:
        db: kaimono-postgres
    spec:
      containers:
        - name: kaimono-postgres
          image: postgres:15.1
          env:
            - name: POSTGRES_USER
              value: user
            - name: POSTGRES_PASSWORD
              value: password 
            - name: POSTGRES_DB
              value: kaimonodb_catalog
          resources:
            requests:
              cpu: 100m
              memory: 60Mi
            limits:
              cpu: 200m
              memory: 120Mi

---

apiVersion: v1
kind: Service
metadata:
  name: kaimono-postgres
  labels:
    db: kaimono-postgres
spec:
  type: ClusterIP
  selector:
    db: kaimono-postgres
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432
